# dev / local
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.env
.env.example
.DS_Store
.tmp/
dist/
.playwright-mcp/
.python-version

# raw scraping artefacts and database stay local; only the exported web/ ships
data/

# Dev-only Python pipeline (scraper, importer, LLM enrichment, FastAPI trainer).
# Kept locally for reproducibility but not part of the deployed static site.
src/
tools/
pyproject.toml
uv.lock
SPEC.md

# logs
*.log
